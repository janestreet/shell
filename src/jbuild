(library
 ((name shell)
  (public_name shell)
  (libraries (console
              core
              filename_extended
              low_level_process
              re2
              shell_internal
              string_extended
              unix_extended))
  (preprocess (pps (ppx_jane ppxlib.runner)))))

(jbuild_version 1)
