(library
 ((name shell)
  (public_name shell)
  (preprocess (pps (ppx_jane -dont-check-doc-comments-attachment ppxlib.runner)))
  (libraries (textutils.console
              core
              filename_extended
              low_level_process
              re2
              shell_internal
              string_extended
              unix_extended))))

(jbuild_version 1)
